<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Available Vaccines - VaccineConnect</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f8f9fa;
        }

        nav ul {
            list-style-type: none;
            margin: 0;
            padding: 0;
            background-color: #2e8b57;
            display: flex;
            justify-content: center;
            padding: 10px;
        }

        nav ul li {
            margin: 0 20px;
        }

        nav ul li a {
            color: white;
            text-decoration: none;
            font-weight: 600;
            transition: color 0.3s ease;
        }

        nav ul li a:hover {
            color: #ffcd3c;
        }

        .category-list {
            display: flex;
            justify-content: center;
            margin: 40px 0;
            gap: 15px;
        }

        .category-list a {
            padding: 12px 25px;
            border-radius: 25px;
            background-color: #ffcd3c;
            color: #2e8b57;
            text-decoration: none;
            font-weight: 500;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

        .category-list a:hover {
            background-color: #ffb320;
            transform: scale(1.1);
        }

        .active-category {
            background-color: #2e8b57;
            color: white;
        }

        .container {
            width: 90%;
            margin: auto;
            text-align: center;
        }

        h1 {
            color: #2e8b57;
            font-weight: 600;
            margin-bottom: 30px;
        }

        form {
            display: flex;
            justify-content: center;
            margin-bottom: 30px;
        }

        form input[type="text"] {
            padding: 10px;
            width: 300px;
            border: 1px solid #ccc;
            border-radius: 25px;
            outline: none;
            font-size: 16px;
        }

        form button {
            background-color: #ffcd3c;
            border: none;
            padding: 10px 20px;
            border-radius: 25px;
            margin-left: 10px;
            cursor: pointer;
            font-weight: 600;
            color: #2e8b57;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

        form button:hover {
            background-color: #ffb320;
            transform: scale(1.1);
        }

        #vaccineList {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 30px;
            padding: 20px;
        }

        #vaccineList li {
            list-style-type: none;
            background-color: white;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
            overflow: hidden;
        }

        #vaccineList li:hover {
            transform: translateY(-10px);
        }

        #vaccineList li img {
            width: 100%;
            height: 150px;
            object-fit: contain;
            background-color: #f0f0f0;
            /* Optional: Add a background color for padding areas */
        }

        #vaccineList li a {
            display: block;
            padding: 20px;
            text-align: center;
            color: #2e8b57;
            font-weight: 600;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        #vaccineList li a:hover {
            color: #ffb320;
        }

        #vaccineList li p {
            padding: 0 20px 20px;
            color: #555;
        }

        /* Add animat
        ions */
        @keyframes fadeIn {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .container,
        .category-list,
        #vaccineList li {
            animation: fadeIn 1s ease;
        }
    </style>
</head>

<body>
    <!-- Navigation Bar -->
    <nav>
        <ul>
            <li><a href="/vaccines">Home</a></li>
            <li><a href="/my-appointments">Appointments</a></li>
            <li><a href="/notifications">Notifications</a></li>
            <li><a href="/profile">Profile</a></li>
            <li><a href="/insurance">Insurance</a></li>
            <li><a href="/vaccine-info">Vaccine Awareness</a></li>
            <li><a href="/logout">Logout</a></li>
        </ul>
    </nav>

    <!-- Category Selection -->
    <div class="category-list">
        <a href="/vaccines?category=Adult" class="<%= category === 'Adult' ? 'active-category' : '' %>">Adult</a>
        <a href="/vaccines?category=Babies" class="<%= category === 'Babies' ? 'active-category' : '' %>">Babies</a>
        <a href="/vaccines?category=Women" class="<%= category === 'Women' ? 'active-category' : '' %>">Women</a>
        <a href="/vaccines?category=Elderly" class="<%= category === 'Elderly' ? 'active-category' : '' %>">Elderly</a>
    </div>

    <!-- Main Container -->
    <div class="container">
        <h1>Available Vaccines</h1>

        <!-- Search Bar -->
        <form method="GET" action="/vaccines">
            <input type="text" id="searchBar" name="search" placeholder="Search by vaccine name...">
            <input type="hidden" name="category" value="<%= category %>"> <!-- Hidden field to keep category filter -->
            <button type="submit">Search</button>
        </form>

        <!-- Vaccines List -->
        <ul id="vaccineList">
            <% if (vaccines.length> 0) { %>
                <% vaccines.forEach(vaccine=> { %>
                    <li>
                        <a href="/vaccines/<%= vaccine.vaccine_id %>/hospitals">
                            <img src="<%= vaccine.image_url %>" alt="<%= vaccine.vaccine_name %> image"
                                onerror="this.src='/images/default-vaccine-icon.png';">
                            <%= vaccine.vaccine_name %> - <%= vaccine.vaccine_type %>
                        </a>
                        <p>Manufacturer: <%= vaccine.manufacturer %>
                        </p>
                    </li>
                    <% }); %>
                        <% } else { %>
                            <p>No vaccines found for this category.</p>
                            <% } %>
        </ul>
    </div>
</body>

</html>